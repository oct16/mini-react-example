function add(x: number, y: number) {
    return x + y
}
import { expect } from 'chai'

describe('should ....', () => {
    it('1 + 1 equal 2', () => {
        expect(add(1, 1)).to.be.equal(2)
    })
})
